overwrite: true
schema: "./graphql/schemas/main-cms.graphql"
documents: "src/**/*.graphql"
generates:
  ./graphql/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
