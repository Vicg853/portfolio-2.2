overwrite: true
generates:
  ./graphql/generated/gql-cms.ts:
    documents: "src/**/*.cms.graphql"
    schema: ${MAIN_CMS_GQL_URL_SCHEMA}
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
